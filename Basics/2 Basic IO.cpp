#include<bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    double b;
    char c;
    cin >> a >> b >> c;
    cout << a << " " << b << " " << c << endl;

    // cin keeps taking data in a variable until it encounters ' ' or '\n'

    // However char always take inside a single thing  like if input 123 it will take 1 the next char
    // will take 2

    return 0;
}